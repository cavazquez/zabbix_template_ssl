UserParameter=ssl.commonName[*],python3 cert.py --hostname $1 --port $2 | awk '/^( commonName).*:/ { s = ""; for (i = 3; i <= NF; i++) s = s $i " "; print s }'
UserParameter=ssl.notAfter[*],python3 cert.py --hostname $1 --port $2 | awk '/^(notAfter).*:/ { s = ""; for (i = 3; i <= NF; i++) s = s $i " "; print s }'
UserParameter=ssl.notBefore[*],python3 cert.py --hostname $1 --port $2 | awk '/^(notBefore).*:/ { s = ""; for (i = 3; i <= NF; i++) s = s $i " "; print s }'
UserParameter=ssl.serial[*],python3 cert.py --hostname $1 --port $2 | awk '/^(Serial).*:/ { s = ""; for (i = 3; i <= NF; i++) s = s $i " "; print s }'
UserParameter=ssl.version[*],python3 cert.py --hostname $1 --port $2 | awk '/^(Version).*:/ { s = ""; for (i = 3; i <= NF; i++) s = s $i " "; print s }'
